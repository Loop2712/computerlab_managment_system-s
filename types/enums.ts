export enum Role {
    Admin = 'Admin',
    Student = 'Student',
    Teacher = 'Teacher', 
  }
  
  export enum RoomType {
    Lecture = 'Lecture',
    Lab = 'Lab',
  }
  
  export enum BookingStatus {
    Pending = 'Pending',
    Approved = 'Approved',
    Denied = 'Denied',
  }
  
  export enum TransactionType {
    Borrow = 'Borrow',
    Return = 'Return',
  }
  